import FindUs from "@/components/FindUs";

// /events IS Find Us now — one chronological road of stops AND events. The route stays
// alive for every link already shared.
export default function EventsScreen() {
  return <FindUs />;
}
