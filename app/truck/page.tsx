import FindUs from "@/components/FindUs";

// /truck IS Find Us now — stops and events are one road (field_ops). The route stays
// alive because QR codes and links in the wild point here.
export default function TruckScreen() {
  return <FindUs />;
}
